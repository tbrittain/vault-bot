import React from 'react';
import { AppBar } from '@material-ui/core';
import './Header.css';

function Header() {
    return (
        <header>
            <AppBar>
                
            </AppBar>
        </header>
    );
}

export default Header;